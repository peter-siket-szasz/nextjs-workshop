type Props = {
  text: string;
  fontSize: string | { [key: string]: string };
};

export default function FancyHeading({ text, fontSize }: Props) {
  // TODO: Create a a fancy heading. See for example: https://chakra-ui.com/docs/components/heading
  // Make use of the props passed to this component, how can you do that?
  // Be creative!
  return <h1>Implement me!</h1>;
}
