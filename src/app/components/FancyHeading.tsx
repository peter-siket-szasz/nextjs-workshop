type Props = {
  text: string;
  fontSize: string | { [key: string]: string };
};

export default function FancyHeading({ text, fontSize }: Props) {
  // TODO: Create a a fancy heading
  return <></>;
}
